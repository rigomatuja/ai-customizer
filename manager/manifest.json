{
  "id": "manager",
  "name": "AI Customizer Manager",
  "description": "Creates, improves, versions, and classifies customs in the AI Customizer catalog.",
  "type": "agent",
  "category": "system",
  "scope": "global",
  "versions": [
    {
      "version": "0.1.0",
      "createdAt": "2026-04-22T00:00:00Z",
      "changelog": "Initial release — catalog-aware manager for skills, agents, and patches."
    },
    {
      "version": "0.2.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Project inference from cwd (suggestion mode); patch auto-detection with .original baseline + blocking validations; gentle-ai detection via <!-- gentle-ai:* --> tag scan; guided exploration protocol (no subagent delegation, read-only outside write scope); agent creation collaborative checklist (Paso 2.10)."
    },
    {
      "version": "0.3.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Skill creation now produces proper YAML frontmatter per tool: Claude gets full schema (name, description, optional when_to_use/paths/allowed-tools/etc.) for semantic auto-activation and paths-based triggers; Opencode gets reduced schema (name, description, optional license/compatibility/metadata) — Claude-only fields stripped from the mirror to avoid misleading readers. New Paso 2.11 collaborative questions for skill frontmatter (description + optional paths). Paso 4.3 rewritten with the mandatory frontmatter block and writing guidance (front-load use case, 1536-char budget, paths is Claude-only)."
    },
    {
      "version": "0.4.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Paso 2.11 rewritten: manager no longer asks the user for the skill's description or paths verbatim. Instead it gathers requirements conversationally (what the skill does, when it fires, file-match vs semantic scope), DRAFTS the frontmatter itself, and PROPOSES it for field-level confirmation/correction before Show-before-write. Same propose-don't-decide pattern as patch auto-detection and project inference. Draft rules added: description front-loads WHEN + WHAT under 180 chars target, paths inferred from stated trigger only (narrow first), Claude and Opencode descriptions stay identical."
    },
    {
      "version": "0.5.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Three simultaneous changes to make the manager unambiguously English and collaboratively universal. (1) Language fix: all 'Paso'/'pasos' (Spanish) renamed to 'Step'/'steps' throughout both tool bodies and the intro index. No other Spanish leaks remained after audit. (2) New Step 0.7 Language policy: artifacts you write to disk are ALWAYS simple English (non-negotiable); communication WITH the user uses the user's language (English artifact fragments stay verbatim inside a non-English explanation). (3) New Step 1.7 Universal propose-don't-decide pattern: codifies the five-beat arc (free expression → incremental questions with free-expression fallbacks → draft + propose → confirm/correct iterate → persist) that ALL Step 2 operations now follow uniformly. Step 2.8 Analyst mode rewritten to invoke the universal arc explicitly for improve/classify/adapt-to-tool/delete, each with its own concrete proposal shape (diff-style summary per op)."
    },
    {
      "version": "0.6.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Agent invocation symmetry across tools. Step 2.10 checklist grows from 9 to 11 dimensions: (10) Opencode mode primary vs subagent — asked explicitly instead of always defaulting to primary; (11) Claude slash-command companion — opt-in second asset `v<ver>/claude/slash-command.md` that the installer copies to `<claude>/commands/<id>.md`. Step 4.4 agent template rewritten with the new optional companion file, its minimal body shape (delegates to the subagent), and a cross-tool invocation symmetry table pairing slash-command with mode: primary and bare subagent body with mode: subagent. No manifest schema change — installer detects the companion by file presence. The general installer now handles multi-asset agents, retiring the previous 'slash commands are a manager-only privilege' gap."
    },
    {
      "version": "0.7.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Per-agent model assignment. Step 2.10 grows to 12 dimensions: new dim 12 asks for the model on Claude (alias/full-id/inherit, from the static catalog registry at .ai-customizer/models/claude.json) AND on Opencode (provider/model-id from the detected registry populated via Settings → Models → Refresh). Default on both: omit the field (inherit from primary). Step 4.4 frontmatter templates updated with optional `model:` field for each tool. Manager NEVER invents model IDs that are not in the relevant registry. Model changes from the UI patch-bump the agent's version (new responsibility documented but the change is server-side, not manager-side)."
    },
    {
      "version": "0.7.1",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Audit fixes on v0.7.0. (1) Step 0.3 READ list explicitly allows `~/.config/ai-customizer/opencode-models.json` (without this, the manager could not read the detected Opencode registry for dim 12 per the 'never touch state dir beyond config.json' rule). (2) Step 2.10 dim 12 rewritten to instruct FILESYSTEM reads of both registries (catalog-side Claude JSON, state-side Opencode JSON) instead of HTTP endpoint references — the manager has no HTTP client, only filesystem tools. The `GET /api/tools/...` endpoints remain documented as the UI's view of the same data, useful to reference when talking to the user but NOT the manager's action path."
    },
    {
      "version": "0.8.0",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Gentle-ai capability enumeration. Step 0.3 READ list expanded to cover the tool-side skill/agent/command dirs (~/.claude/skills/**, ~/.claude/agents/**, ~/.claude/commands/**, ~/.config/opencode/skills/**, ~/.config/opencode/agent/**) — read-only, never write. Step 3.9 rewritten as a two-phase protocol: Phase 1 does the existing tag scan (install detection); Phase 2 enumerates gentle-ai's installed skills, slash commands, and agents via filesystem for reference during authoring. Guidance on how to use the enumeration in skill/agent/patch authoring: body-reference gentle-ai skill IDs by exact match; do NOT add them to dependencies.customs (that's catalog-internal); dependencies.gentleAi.required = true is the correct runtime flag. New Step 3.4.g documents patches targeting gentle-ai tag sections (the primary patch use case). Light touches in 2.10 and 2.11 nudge the manager to run the enumeration before Show-before-write when the user's intent overlaps with what gentle-ai provides. A TAG IS NOT A SKILL ID is emphasized throughout — previous versions confused the two."
    },
    {
      "version": "0.8.1",
      "createdAt": "2026-04-23T00:00:00Z",
      "changelog": "Audit fixes on v0.8.0. Three changes. (1) Step 3.9 gains an explicit 'Trigger vocabulary' section enumerating the signals (direct, indirect, discovery, composition) that license running each phase — and a hard rule: NEVER run Phase 1 unsolicited. 'Intent overlaps with gentle-ai' is removed as a license; the user must have given a signal. (2) New Phase 3 — deep-read of a specific skill/command/agent body when the user picks it to compose with. Phase 2 stays shallow (frontmatter only); Phase 3 reads full body and extracts whenToInvoke / whatItDoes / output / rules / anti-patterns so the manager can reference the capability precisely in the user's custom. (3) Phase 2 presentation now groups skills by natural prefix (sdd-*, pr-*/issue-*, review, etc.) and separates ambient tags from invokable skills from user-triggered slash commands. A new terminology glossary at the top of 3.9 makes the tag/skill/command/subagent distinction explicit. Steps 2.10 and 2.11 composition-check paragraphs rewritten to reference the trigger vocabulary — they no longer push the manager to run enumeration on mere 'intent overlap'."
    },
    {
      "version": "0.8.2",
      "createdAt": "2026-04-25T00:00:00Z",
      "changelog": "Catch up with current Opencode spec — `mode: all` is now a first-class option alongside `primary` and `subagent` (Opencode docs at opencode.ai/docs/agents). Step 2.10 dim 10 rewritten with three explicit options: `primary` (Tab-only), `subagent` (delegation-only, also @agent), `all` (both). Default lean when the user has no preference flips from `subagent` to `all`, matching Opencode's own omit-default and giving maximum flexibility — the user can tighten later if needed. Step 4.4 Opencode frontmatter template updated to `mode: primary | subagent | all`. Cross-tool invocation symmetry table expanded from 2 rows to 3 (direct-only / delegation-only / both) with the matching Claude side per row (companion or bare body). The earlier 'always primary by default' stub in the §0.2 mode-per-tool reference also corrected to the three-way list. Pure documentation/protocol fix — installer doesn't validate frontmatter mode, so no installer change needed."
    },
    {
      "version": "0.9.0",
      "createdAt": "2026-04-25T00:00:00Z",
      "changelog": "Show-before-write becomes opt-in. Step 1.6 rewritten as a 'Pre-write protocol' with two paths chosen by the user: (A) show full artifacts in code blocks before writing, then approve and write; (B) skip the show step and write directly. The manager asks ONCE per write batch when it reaches write-ready state ('Show the full artifacts first, or go straight to writing?'). Pre-declared preferences ('just write', 'no necesito ver', 'always show me', etc.) skip the question and apply for the rest of the conversation. Path A and path B both ALWAYS announce the absolute paths written and remind the user to run Apply. Previous v0.8.x and earlier mandated the show step ('Do NOT skip the show step because the user will trust me') — that hard rule is replaced by 'asking is mandatory unless the user pre-declared'. Body retains the 'Show-before-write' label as a colloquial pointer to 1.6 from older callers."
    }
  ],
  "activeVersion": "0.9.0"
}
